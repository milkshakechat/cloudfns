import { StoryID, UserID } from "../types/base.types";
export declare enum ImageResizeOption {
    "thumbnail" = "200x200",
    "compressed" = "768x768"
}
export declare enum BucketFolderSlug {
    "avatars" = "avatars/",
    "story_image" = "story/image/"
}
export declare function getCompressedAvatarUrl({ userID, assetID, size, bucketName, }: {
    userID: UserID;
    assetID: string;
    size?: ImageResizeOption;
    bucketName: string;
}): string;
export declare function getCompressedStickerUrl({ userID, assetID, size, bucketName, }: {
    userID: UserID;
    assetID: string;
    size?: ImageResizeOption;
    bucketName: string;
}): string;
export declare function getCompressedWishlistGraphicUrl({ userID, assetID, size, bucketName, }: {
    userID: UserID;
    assetID: string;
    size?: ImageResizeOption;
    bucketName: string;
}): string;
export declare function getCompressedStoryImageUrl({ storyID, userID, size, assetID, bucketName, }: {
    storyID: StoryID;
    userID: UserID;
    assetID: string;
    size: ImageResizeOption;
    bucketName: string;
}): string;
